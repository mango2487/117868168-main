#include <stdio.h>
#include <cs50.h>
#include <string.h>

int main(void)
{
    string word1 = get_string("Scrabble game! choose your word: ");
    printf("%s\n");
}

if(score1>score2)
    {
        printf("Player 1 is the winner!");
    }
    else if(score1<score2)
    {
        printf("Player 2 is the winner!");
    }
    else(score1==score2)
    {
        printf("Tied!");
    }